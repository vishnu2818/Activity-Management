<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_users_form_inherit_ah_activities_mgmt_basic" model="ir.ui.view">
        <field name="name">res.users.view.form.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="check_user_activity" invisible="1"/>
                <field name="check_user_activity_type" invisible="1"/>
            </xpath>
        </field>
    </record>

</odoo>